# Handoff — 2026-09-05

## What changed
- Added `CLAUDE.md` (project layout, verdict engine, deployment, gotchas).
- Fixed two inaccuracies:
  - `README.md` said `cd dist/tankmate` before serving locally — there is no `dist/` directory;
    the app files are in the repo root. Removed that line.
  - `CLAUDE.md` cited the verdict engine as "app.js lines 21–55"; the actual `RULES` block runs
    lines 23–45 in the current file. Corrected.

## What I verified against the actual repo
- `.github/workflows/deploy.yml` confirmed: rsyncs to
  `deploy@joanmarcriera.es:/opt/stacks/core/tankmate/` on push to `main` — matches CLAUDE.md's
  deployment section exactly.
- `sw.js` line 2 `VERSION = 'tankmate-v1'` — matches the cache-key gotcha.
- `ROTATION` (line 129) and `PLAYBOOK` (line 151) array line numbers in app.js match CLAUDE.md's
  "Quick edits" pointers exactly.

## Left for Marc
- None outstanding.
